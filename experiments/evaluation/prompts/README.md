# Prompt Sets

After the held-out test split is frozen, record the prompt-selection procedure
and the resulting prompt text here. Keep selection independent of model
outputs. The primary plan calls for stratified dialogue, speaker labels, stage
directions, and short and long continuations.
