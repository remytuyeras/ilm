# Frozen Experiment Tokenizers

Place the tokenizer JSON fitted from the experiment's frozen source corpus here,
alongside a manifest with its source and tokenizer checksums. Keep embedding
caches outside version control, for example under `data/cache/`.
